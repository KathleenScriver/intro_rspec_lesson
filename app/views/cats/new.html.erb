<h1>Add a Cute, Cuddly Kitty!</h1>

<%= form_for @cat do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :breed %>
  <%= f.text_field :breed %>

  <%= f.submit %>

<% end %>


<%= link_to "Abandon Cat!", cats_path %>
